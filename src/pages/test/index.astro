---
import fetch from "node-fetch";

// Strapiからデータをフェッチする関数
async function getBlogs() {
	const response = await fetch(
		"https://usable-charity-13cbbf6393.strapiapp.com/api/blogs",
	);
	const data = await response.json();
	console.log(data);
	return data.data;
}

// ページがロードされる時にデータをフェッチ
const blogs = await getBlogs();
---

<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Blog Posts</title>
	</head>
	<body>
		<h1>Blog Posts TestTest</h1>
		<ul>
			{blogs.map((blog) => <li>{blog.attributes.title}</li>)}
		</ul>
	</body>
</html>
